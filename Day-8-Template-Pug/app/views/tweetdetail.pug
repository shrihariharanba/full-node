doctype html
html(lang='en')
  head
    meta(charset='utf-8')
    meta(http-equiv='X-UA-Compatible', content='IE=edge,chrome=1')
    title Tweet Detail
  body
    section.container
        P #{tweet.message}
        P #{tweet.name}
        P #{tweet.userId}
        p
         a(href='http://localhost:3002/tweet/v1/detail/update/'+tweet.id) update
         form(method='post', action='http://localhost:3002/tweet/v1/delete/'+tweet.id)
          input(type='submit', name='', value='delete')